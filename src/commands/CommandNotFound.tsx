export default function CommandNotFound (){
    return( <div key="command-not-found">
            Command not found. Type{" "}
            <span className="accent">help</span> to find all
            available commands.
          </div>)
}